@using Exam_Objective.Models
@{
    ViewBag.Title = "Exportout";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="panel-heading">
    @{var ObjectiveModelEntity = (List<ObjectiveModel>)ViewBag.Objective;

        foreach (var rows in ObjectiveModelEntity)
        {
            <h3><span class="icon icon-login"> ส่งออกข้อสอบของจุดประสงค์ </span>@rows.ObjName</h3>
        }
    }
</div>
<div class="panel panel-default">

    <div class="panel-heading">
        เลือกประเภทไฟล์ข้อสอบ
    </div>
    <div class="panel-body">
        <div class="panel-body">
                 XMLFILE
                 GITGFILE
        </div>
    </div>
</div>

@section scripts{

}